# 왜 Spring을 사용할까?
- 객체의 생성과 의존성 관리를 개발자가 직접 하지 않도록 하기 위해서.
Spring은 DI(Dependency Injection)이라는 기능으로 프레임워크 자체에서 
객체의 생성과 의존성 주입을 한다.

- `new`를 통해 직접 객체를 생성하는 방식이 단순해서 쉽게 느껴질 수 있지만
추후 코드수정이 발생했을 때 DI방식보다 코드수정량이 더 많다. (변경에 취약하다)

- 테스트 시 Mock객체를 주입하여 사용하는 등 단위테스트에 쉬운 구조를 만들 수 있다.
