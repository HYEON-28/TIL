# Chapter 1 - A Tutorial Introduction

컴파일 방법

```
  gcc hello.c -o hello
```

arithmetic: 산수, 기초 계산
extrapolate: 이미 알고있는 정보나 패턴을 바탕으로 앞으로의 값, 결과를 추정하다

### \*\*\*\*정의되지 않은 동작(Undefined Behavior)이란?

> printf("Hello, World!\c");

같은 상황.
정의되지 않은 동작은 컴파일러를 만드는 사람이 임의로 처리 가능함.

- 경고만 띄우고 c를 출력
- \n 를 출력
- 에러처리

## 1.2 Variables and Arithmetic Expressions

14p부터 하기
