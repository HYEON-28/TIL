# 여러 조인들

## 1. INNER JOIN (교집합)
각 A, B 테이블에서
조건을 만족하는 행들만 남음.

## 2. OUTER JOIN (합집합)
LEFT OUTER JOIN(LEFT JOIN)
왼쪽 테이블은 전체, 오른쪽 테이블은 조건을 만족하는 행만 출력

## 3. FULL OUTER JOIN
양쪽 테이블 모두 출력, 매칭 안되는 쪽은 NULL으로 

## 4. CROSS JOIN
조건 없음, 두 테이블의 모든 조합 생성