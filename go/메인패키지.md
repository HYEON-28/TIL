# 메인패키지

- 컴파일을 하기위해서는 파일이름이 main이어야 한다 (main.go)
- 컴파일을 하지 않아도 된다면(라이브러리 작성 등) 파일이름이 main일 필요는 없다
- main.go -> 컴파일러의 진입점

함수 작성하는 법 - My First Go App

```
package main

import "fmt"

func main() {
	fmt.Println("Hello world!")
}
```
